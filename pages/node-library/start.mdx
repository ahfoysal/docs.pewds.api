import { Tab, Tabs } from "nextra-theme-docs";

# Get Started

The "pewds-api" library, otherwise @pewds/api, is available via mainstream package managers.

## Installation

Instructions for installing the library can be found below.

{/* NOTE: do NOT run a formatter for the time being; it breaks the code blocks below. */}

<Tabs items={["npm", "yarn"]}>
  <Tab>
  ```bash
  npm i 
  ```
  View package at [npmjs.com](https://www.npmjs.com/package/)
  </Tab>
  <Tab>
  ```bash
  yarn add 
  ```
  View package at [yarnpkg.com](https://yarnpkg.com/package/)
  </Tab>
</Tabs>
